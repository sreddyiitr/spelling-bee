# spelling-bee

Tiny example Python package with a minimal spelling check function and CLI.

Quick start

Create a virtualenv (recommended):

```bash
source venv/bin/activate
pip install flask
python app.py
```
